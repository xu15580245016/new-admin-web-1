<!---->
<template>
    <h4>页面走丢了</h4>
    <!--    <p>将在{{ count }}秒后跳转至首页</p> -->
</template>

<script setup>
/* import { onMounted, ref, onBeforeUnmount } from 'vue'
import { useRouter } from 'vue-router';
const router = useRouter()
const count = ref(3)
let timer = () => {
    // 函数体
    setInterval(() => {
        // 函数体
        // count.value--
        if (count.value === 0) {
            count.value = 3
            router.push('/home')
        } else {
            count.value--
        }
    }, 1000)
}
onMounted(() => {
    // 函数体
    timer()
})
onBeforeUnmount(() => {
    // 函数体
    clearInterval(timer)
}) */

</script>

<style scoped></style>